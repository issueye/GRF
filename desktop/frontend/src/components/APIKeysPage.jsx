import { KeyRound, Plus, Trash2 } from 'lucide-react';
import { useState } from 'react';

export function APIKeysPage({ apiKeys, busy, onCreate, onDelete, onToggle }) {
  const [name, setName] = useState('');
  return <section className="page" aria-labelledby="keys-title"><div className="page-heading"><div><h1 id="keys-title">API Keys</h1><p>调用本机兼容 API 时使用。密钥正文仅在创建时显示一次。</p></div></div><form className="key-create" onSubmit={(e) => { e.preventDefault(); if (name.trim()) { onCreate(name.trim()); setName(''); } }}><label><span>名称</span><input onChange={(e) => setName(e.target.value)} placeholder="例如：本机客户端" value={name} /></label><button className="button button-primary" disabled={busy || !name.trim()} type="submit"><Plus size={14} /> 创建密钥</button></form>{!apiKeys.length ? <div className="empty-state table-shell"><KeyRound size={24} /><strong>尚未创建 API Key</strong><span>创建后请立即保存弹窗中的完整密钥。</span></div> : <div className="gateway-table keys-table"><div className="gateway-table-head"><span>名称</span><span>密钥前缀</span><span>状态</span><span>操作</span></div>{apiKeys.map((key) => <div className="gateway-table-row" key={key.id}><div><strong>{key.name}</strong><small>{key.last_used_at ? `最近使用 ${new Date(key.last_used_at).toLocaleString()}` : '尚未使用'}</small></div><code>{key.prefix}...</code><label className="compact-toggle"><input checked={key.enabled} onChange={(e) => onToggle(key.id, e.target.checked)} type="checkbox" /><span>{key.enabled ? '已启用' : '已停用'}</span></label><button className="icon-button bordered danger-icon" onClick={() => onDelete(key.id)} title="删除密钥" type="button"><Trash2 size={14} /></button></div>)}</div>}</section>;
}
